import { Box } from "@mui/material";
import React from "react";
import OutlinedCard from "../ui/FeedCard";

const Feed = () => {
  return (
    <Box flex={4} p={2}>
      <OutlinedCard />
    </Box>
  );
};

export default Feed;
